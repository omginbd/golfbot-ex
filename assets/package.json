{
  "devDependencies": {
    "esbuild": "^0.12.26",
    "esbuild-plugin-sass": "^0.6.0",
    "ncp": "^2.0.0"
  },
  "scripts": {
    "deploy": "cd .. && mix assets.deploy && rm -f _build/esbuild"
  }
}
